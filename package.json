{
  "name": "mongoose-history",
  "description": "Mongoose plugin that adds createdAt and updatedAt auto-assigned date properties. Then it creates a history object which holds a copy of the current state.",
  "version": "0.5.0",
  "author": "Publicis <andrew.apperley@publicis.ca>",
  "keywords": [
    "mongodb",
    "mongoose",
    "plugin",
    "timestamps",
    "createdAt",
    "updatedAt",
    "history"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/drudge/mongoose-timestamp"
  },
  "dependencies": {
    "captain-hook": "0.0.3"
  },
  "devDependencies": {
    "mongoose": "~3.8.8",
    "should": "~3.2.0",
    "mocha": "~1.18.2"
  },
  "scripts": {
    "test": "mocha -u bdd -R spec -c ./test/*test.js"
  },
  "license": "MIT",
  "engine": {
    "node": ">= 0.6"
  }
}
